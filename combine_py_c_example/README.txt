In RPi command line, type
    sudo python setup.py install
If this causes errors, install the python-dev directory using
    sudo apt-get install python-dev
    
Once this succeeds, run program using
    python cextension.py